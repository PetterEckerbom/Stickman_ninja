extends header

block content
    form(method='POST', action='/users/login')
      h1 login
      lable Username
      input(name='username', type='text')
      br
      lable password
      input(name='password', type='password')
      br
      input(type='submit')
